import type { FieldName, ScalarValue } from "../contracts";
const moneyFields=new Set<FieldName>(["income.amount","assets.total","assets.bank_balance","debts.monthly","property.purchase_price","property.appraised_value","loan.amount","housing.insurance","housing.taxes","housing.hoa","assets.reserves"]);
export function normalizeValue(field:FieldName,raw:string):ScalarValue {const clean=raw.normalize("NFKC").replace(/\s+/g," ").trim();if(moneyFields.has(field)){const negative=/^\(.*\)$/.test(clean);const parsed=Number(clean.replace(/[^0-9.-]/g,""));return Number.isFinite(parsed)?Math.round((negative?-Math.abs(parsed):parsed)*100)/100:null}if(field==="credit.score"){const n=Number(clean);return Number.isInteger(n)?n:null}if(field==="loan.interest_rate"){const n=Number(clean.replace(/%/g,""));return Number.isFinite(n)?Math.round(n*10000)/10000:null}if(field==="borrower.ssn")return clean.replace(/\D/g,"");if(field==="borrower.dob"){const m=clean.match(/^(\d{1,2})[\/-](\d{1,2})[\/-](\d{4})$/);return m?`${m[3]}-${m[1].padStart(2,"0")}-${m[2].padStart(2,"0")}`:clean}return clean.toLocaleLowerCase("en-US")}
export function valuesEqual(a:ScalarValue,b:ScalarValue):boolean{return typeof a===typeof b&&a===b}
