import { createHash } from "node:crypto";import { EVIDENCE_SCHEMA_VERSION,type EvidenceInput,type EvidenceObject,type EvidenceResult } from "../contracts";import { validateEvidenceInput } from "../validation/validator";
const hash=(x:unknown)=>createHash("sha256").update(JSON.stringify(x)).digest("hex");
export function createEvidence(input:EvidenceInput):EvidenceResult<EvidenceObject>{const errors=validateEvidenceInput(input);if(errors.length)return Object.freeze({ok:false,errors});const region=Object.freeze({...input.boundingRegion});const fingerprint=hash([input.documentId,input.pageNumber,region,input.ocrText]);const evidenceId=hash([EVIDENCE_SCHEMA_VERSION,input.fieldName,input.normalizedValue,input.documentId,input.borrowerId,input.pageNumber,region,input.extractionMethod,input.sourceComponent,input.version,fingerprint]);return Object.freeze({ok:true,value:Object.freeze({...input,boundingRegion:region,schemaVersion:EVIDENCE_SCHEMA_VERSION,evidenceId,validationStatus:"valid",provenance:Object.freeze({sourceDocumentId:input.documentId,sourcePageNumber:input.pageNumber,sourceRegion:region,contentFingerprint:fingerprint})})})}
